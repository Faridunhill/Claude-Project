{
  "state_dir": "state",
  "channel_dir": "~/Desktop/FARID_CLAUDE_CHANNEL/channel",
  "ark_dir": "~/Desktop/PIPE_LIBRARY_SHARED_2026-07-18",
  "gpu_allclear": false,
  "_notes": {
    "channel_dir": "Where the cloned Claude-Project repo's channel/ folder lives on this PC.",
    "ark_dir": "Read-only to the Manager. No registry command touches it.",
    "gpu_allclear": "Farid flips this to true when the GPU is free. Until then, needs_gpu jobs are never handed out."
  }
}
