# DECISIONS — what is dead, what is locked
Opened 2026-07-31. Authority: Farid, in session. Nothing enters this file that he did
not rule. A future session reads this instead of re-litigating.

---

## KILLED — authorized 2026-07-31, "finish all the above"

| # | Killed | Why it is dead |
|---|---|---|
| 1 | **The MoneyPrinter-class video generator, as a footage source** | It builds videos from **Pexels stock footage of other people's pipes** while we own 2,000+ photos of ours. **Its composer survives** — captions, TTS, MoviePy assembly, brand palette — reused as Episode Engine stage 5, pointed at our own panels. *Kill the input, keep the machine.* |
| 2 | **Blockchain / NFT provenance** | The value is the database and the trust, not the token. A stable reference ID gives the same story at zero cost and no crypto friction. |
| 3 | **White-label API · insurance partnerships · authentication academy** | All "sell the method" before the method has volume or a track record. |
| 4 | **"The Proof Log is an invention nobody has"** | Farid's own five-seat red team returned **NO** on the invention claim. The sealed hash-chained log and the guardrails **live** as good engineering; the "first in the world, the clock is running" framing is dead and must not drive urgency. |
| 5 | **The Passport as an authenticity answer** | Authenticity was asked in **1 buyer message out of 1,053.** The machine lives; the promise changes to **condition and originality** (the Dating Desk). |
| 6 | **`rating` and `reviewCount` in the product schema** | A branch already removed fabricated ratings and fake testimonials as "store hygiene." Under LAW 1 these fields are a defect, not a feature. **Open task: strip them from `main`.** |
| 7 | **"Certification" and every synonym** | Permanently prohibited, and found drifting back as "authentication." Kept on this list so it stays dead. |

**Also killed, 2026-07-31 (separate ruling):** *leather as the ad-safe department.* The
platforms refuse the account, not the product — because leather sits in the same store
as pipes. **faridunhill.com carries zero paid promotion on any product, forever.**
Ad-safe is a property of an **entity**, not a department: the paid door is Ashcombe.

---

## LOCKED — style and its roadmap

**v1 — NOW: inked 2D comic.** Heavy black outlines, flat cel shading, briar/brass
palette, still panels with a camera move, burned-in captions, voice-over, **no talking
mouths.** Chosen from Farid's reference, for four reasons that still hold: the ink line
performs the subtraction a model cannot; ink forgives anatomy errors that smooth 3D
punishes; consistency rides on costume signatures rather than the face; and nothing has
to lip-sync, which deletes the avatar, the HeyGen cost and frame-to-frame drift at once.

**★ Farid's own words, 2026-07-31 — this is v1, not the destination:**
> *"It was just an idea we can start with… so yes we will start with this simple now,
> but we will not stop improving, and change it later to complete 3D live."*

**v2 — THE DESTINATION: full 3D.** A modelled, rigged Professor and Assistant in a
modelled workshop, where consistency is guaranteed by construction rather than by style
discipline, the camera can move through a real set, and the anatomy model sections
itself at any plane.

### Why v1 does not become wasted work — the architecture protects the upgrade
The Episode Engine composites **layers resolved from an asset library by id**. It does
not generate finished frames. Therefore:

- Stage 3 asks the library for `prof-explaining-3q`. Whether that id resolves to an ink
  drawing or a render from a 3D rig **is invisible to every other stage.**
- Stages 1, 2, 4, 5, 6, 7 — subject, script, voice, assembly, publishing, ledger — never
  change. **Only the asset files behind the ids change.**
- The scripts, panel lists and published episodes stay valid: an old episode can be
  **re-rendered in v2 style from its saved panel list**, with no rewriting.

**Rule that makes this true and must not be broken: no stage may ever hard-code an image
path. Everything goes through an asset id.** That single discipline is what converts
"we will change it later" from a hope into a scheduled upgrade.

The designer commission is written to serve both: a **character sheet with turnarounds,
expressions and shape rules** is exactly the specification a 3D modeller needs. Buying
the design once feeds v1 this month and v2 whenever Farid calls for it.

---

## STANDING (unchanged, restated so nobody reopens them)
- **Subscriptions: LATER.** Free core until collectors come and value it. Farid's gate.
- **Launches, the domain, the museum brand list: Farid's decision alone.**
- **LAW 1 honesty · LAW 2 buy-don't-pirate** sit above everything on this page.
