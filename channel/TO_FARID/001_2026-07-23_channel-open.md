# 001 — The channel is open

Farid — this folder (`channel/TO_FARID/`) is where I leave things for you. You leave things for
me in `TO_AGENT/` (notes, questions) and `NEW_UPLOADS/` (raw folders and files). Full protocol in
`channel/README.md`, including the one-time command to put this channel on your Desktop.

## What to drop into NEW_UPLOADS first (in order of urgency)

1. **The ark's manifest.** The survey confirmed the pipedia/pipephil mirrors exist in *neither*
   repo — the encyclopedia's foundation is untracked. Don't upload the whole mirror yet (it may
   be huge); start with a listing so it's inventoried. In PowerShell, from inside the mirror
   folder (likely `PIPE_LIBRARY_SHARED_2026-07-18`):
   `Get-ChildItem -Recurse | Select FullName, Length | Export-Csv manifest.csv`
   → drop `manifest.csv` here.
2. **The professor's identity files.** Your source photo, the portrait takes, and — the moment
   it's approved — `BIBLE.png`. Right now the character's face exists only on your machine;
   one disk failure and he's gone.
3. **FARID_CHANNEL folder contents** (from your Desktop) — so I can see the channel plans.
4. **BULK_DATING_CORPUS_20260719.md** — looks like it belongs to the Dating Desk / References
   Archive work; I'll file it.

## Where everything stands (short version)

- Brainstorm + face pipeline: `docs/PROFESSOR_PROJECT.md`, `docs/PROFESSOR_FACE_PIPELINE.md`
  (this repo, branch `claude/session-71nyhc`); verified-status copy also on faridunhill-live,
  branch `claude/professor-project-status-hs1tao`.
- Professor's four remaining steps: commit BIBLE.png → HeyGen photo avatar → voice into HeyGen
  → ten-second talking test.

— your agent
